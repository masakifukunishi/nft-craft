{
  "name": "nft-craft",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "install": "npm install --prefix frontend && npm install --prefix hardhat",
    "build": "npm run compile --prefix hardhat && npm run build --prefix frontend"
  }
}
