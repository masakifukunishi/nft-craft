const Spinner = () => {
  return (
    <div className="animate-spin inline-block size-14 border-[5px] border-current border-t-transparent text-blue-500 rounded-full">
      <span className="sr-only">Loading...</span>
    </div>
  );
};

export default Spinner;
